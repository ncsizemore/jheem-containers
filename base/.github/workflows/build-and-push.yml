name: Build Base Image

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/jheem-base

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # DISABLED: Cascade rebuild assumes all model containers use the same jheem-base
  # version. While different models need different jheem2 versions (due to simset/engine
  # version matching), rebuild each model container individually with the correct
  # BASE_VERSION. Re-enable when all simsets converge to a single jheem2 version.
  # See: jheem-portal/docs/CUSTOM-SIMULATIONS-PLAN.md
  #
  # notify-downstream:
  #   needs: build
  #   if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
  #   runs-on: ubuntu-latest
  #   strategy:
  #     matrix:
  #       repo:
  #         - ncsizemore/jheem-container-minimal
  #         - ncsizemore/jheem-ryan-white-croi-container
  #         - ncsizemore/jheem-cdc-testing-container
  #   steps:
  #     - name: Extract version from tag
  #       id: version
  #       run: echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
  #
  #     - name: Trigger rebuild of ${{ matrix.repo }}
  #       uses: peter-evans/repository-dispatch@v3
  #       with:
  #         token: ${{ secrets.DISPATCH_TOKEN }}
  #         repository: ${{ matrix.repo }}
  #         event-type: base-image-updated
  #         client-payload: '{"base_version": "${{ steps.version.outputs.version }}"}'
